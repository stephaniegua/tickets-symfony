<form method="post" action="{{ path('app_responsable_delete', {'id': responsable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responsable.id) }}">
    <button class="btn">Delete</button>
</form>
